{
  "scripts": {
    "publish": "cargo publish",
    "remove": "cargo remove",
    "dev": "cargo run",
    "build": "cargo build",
    "install": "cargo install --path .",
    "add": "cargo add",
    "lint": "cargo fmt --all",
    "clean": "cargo clean"
  }
}