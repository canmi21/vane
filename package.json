{
  "name": "vane",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "docs"
  ],
  "scripts": {
    "check": "bun run lint && cargo fmt --check && cargo clippy -- -D warnings && cd integration && gofmt -l .",
    "fmt": "oxfmt --write . && dprint fmt && cargo fmt --all && cd integration && gofmt -w .",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "prettier": "^3.8.1"
  }
}
