[package]
name = "prom-cardinality-cap"
version = "0.0.1"
categories = ["development-tools::profiling", "asynchronous"]
edition.workspace = true
keywords = ["prometheus", "metrics", "cardinality", "labels", "cap"]
license.workspace = true
readme = "README.md"
repository.workspace = true
rust-version.workspace = true
description = "Per-namespace cap on Prometheus metric label cardinality, with warn-once-on-first-drop semantics."

[lints]
workspace = true

[dependencies]
tracing.workspace = true
