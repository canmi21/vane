{
  "scripts": {
    "remove": "cargo remove",
    "dev": "cargo run",
    "add": "cargo add",
    "build": "cargo build",
    "publish": "cargo publish",
    "clean": "cargo clean",
    "lint": "cargo fmt --all",
    "install": "cargo install --path ."
  }
}