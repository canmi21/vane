---
title: HTTP/1.1
description: The foundation of the web.
icon: Globe
---

import { Step, Steps } from 'fumadocs-ui/components/steps'

## Implementation

**Source**: `src/layers/l7/http/httpx.rs`

Vane uses `hyper` (v1) for robust HTTP/1.1 support.

<Mermaid
  chart="
graph LR
    Client -->|Req| Hyper
    Hyper -->|Parse| Adapter
    Adapter -->|Body| Container
    Container -->|Resp| Adapter
    Adapter -->|Serialize| Hyper
    Hyper -->|Bytes| Client
"
/>

<Steps>
<Step>
### Parsing
Zero-copy header parsing via `httparse` (internal to Hyper).
</Step>

<Step>### Keep-Alive Fully supported and enabled by default to reuse TCP connections.</Step>

<Step>
### Upgrades
Handles `Connection: Upgrade` (specifically for WebSockets) before handing off to the Flow Engine.
</Step>
</Steps>
