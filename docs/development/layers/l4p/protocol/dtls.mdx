---
title: DTLS
description: Datagram Transport Layer Security (Future).
icon: Combine
---

## Roadmap

DTLS support is currently planned for a future release.

The goal is to provide similar inspection capabilities for DTLS (over UDP) as we currently do for TLS (over TCP), enabling SNI-based routing for secure UDP protocols (like WebRTC or CoAP) without full termination.
