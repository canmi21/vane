name: style

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  style:
    name: style
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: false

      - name: Cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: style

      - name: Format
        run: cargo fmt --all -- --check

      - name: Install dprint
        uses: taiki-e/install-action@v2
        with:
          tool: dprint

      - name: Prose
        run: dprint check

      - name: Workspace dep sync
        run: cargo xtask sync-deps check

      - name: Anchors
        run: cargo xtask check-spec-anchors
