{
	"scripts": {
		"dev": "pnpm dev",
		"clean": "pnpm clean",
		"publish": "pnpm publish",
		"build": "pnpm build",
		"add": "pnpm add",
		"lint": "pnpm lint",
		"install": "pnpm install",
		"remove": "pnpm remove"
	}
}
